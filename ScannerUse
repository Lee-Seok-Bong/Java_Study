[자바 char형 입력]



자바 Scanner는 문자를 입력받는 기능이 없다.

즉, Scanner.next()로 문자열을 입력받은 다음, String.charAt(0)으로 첫번째 문자를 꺼내오면 된다.



(1)

String s = scanner.next();
char c = s.charAt(0);
(2)

char c = scanner.next().charAt(0);


위의 두가지 방법을 통해서 char형인 c에 문자를 입력할수 있다.

